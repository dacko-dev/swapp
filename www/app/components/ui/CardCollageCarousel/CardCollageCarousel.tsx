import { Box, em, Image, useMatches } from "@mantine/core";
import { useMediaQuery } from "@mantine/hooks";
import { useState } from "react";
import { TMockItem } from "~/utils/mock";

export default function CardCollageCarousel({ items }: { items: TMockItem[] }) {
  return (
    <Box>
      <Box></Box>
    </Box>
  );
}
